# T2b x402 Discovery Registration — Execution Plan

> Ready-to-run deploy job. Fully de-risked by research 2026-07-26 (below). Not started (deploy step needs a stable public URL). superpowers flow, no vcsdd.

**Goal:** make an x402 endpoint (payTo `0x810F6D61F7606dEEE2657d3083E150a222Bc29C5`, Base mainnet USDC) DISCOVERABLE so an external buyer-agent finds and pays it → unblocks the S12b earning gate and revenue-$0 wall.

## Measured reality (2026-07-26)

| Fact | Evidence |
|---|---|
| 0x810f NOT in Bazaar | `GET https://api.cdp.coinbase.com/platform/v2/x402/discovery/resources` (no auth) → 0 of 14,413 items (research agent, real GET) |
| Prod x402 routes 404 | `anicca-proxy-production.up.railway.app/health`=200 but `/api/x402/<skill>`=404 (curl) — discovery-ready server NOT deployed there |
| Two discovery-ready impls exist, both undeployed | `anicca-rtdash/apps/x402-agents/src/server.js` (CDP facilitator + `declareDiscoveryExtension`, 8 routes, railway.toml, NOT linked); `life-manager-8i-cutover` serve.mjs (x402.org facilitator, not CDP Bazaar) |
| Secrets on disk | `~/.openclaw/.env`: OPENAI_API_KEY, CDP_API_KEY_ID, CDP_API_KEY_SECRET, X402_WALLET_ADDRESS ✅. **DATABASE_URL MISSING** (must provision) |

## Registration mechanics (research-confirmed)

- **x402scan** (cheapest pre-settlement): `POST https://www.x402scan.com/api/x402/registry/register` body `{url}` (or `/register-origin` `{origin}`) + **SIWx wallet signature from 0x810f**. Server PROBES the URL live → needs a stable public https URL returning a valid 402 with the bazaar extension. Source: `Merit-Systems/x402scan` `apps/scan/src/app/api/x402/registry/register/route.ts` (`.siwx()` guard, `registryRegisterBodySchema={url}`).
- x402scan + AgentCash share backend → one registration hits both.
- CDP Bazaar itself = settlement-driven: auto-indexes only AFTER first settle. best: after x402scan register, self-buy 1 call (~$0.005) to also land in CDP Bazaar (INV-7: self-pay is DISCOVERY, not earn).
- Reinforce ($0): PR one line to `michielpost/x402-dev` / `xpaysh/awesome-x402`.

## Tasks

### Task 1: minimal DB-free discoverable server
The full x402-agents server requires DATABASE_URL + OPENAI_API_KEY per route. For discovery we need ONE route that returns real value with NO DB/OpenAI, so the deploy is light and the probe passes. Create `apps/x402-agents/src/server-lite.mjs`: a single `POST /prompt-sanitizer` (pure PII-strip regex — real utility, zero external dep), CDP mainnet facilitator, `declareDiscoveryExtension`, payTo from `X402_WALLET_ADDRESS`, price `$0.005`, `X402_NETWORK=eip155:8453`. Boots with only X402_WALLET_ADDRESS + CDP creds. Unit test: middleware mounts, GET-with-no-payment → 402 body carries `accepts[].payTo==0x810f` + discovery extension. Verify locally: `curl` the route → inspect 402 JSON shape BEFORE deploy.

### Task 2: deploy to a stable https URL
`railway link` (or `railway init`) a dedicated project → set env (X402_WALLET_ADDRESS, CDP_API_KEY_ID, CDP_API_KEY_SECRET, X402_NETWORK=eip155:8453) → `railway up` → get the `*.up.railway.app` URL. Verify: `curl -X POST <url>/prompt-sanitizer` → HTTP 402 with correct body (repeat the Task 1 shape check against the LIVE url).

### Task 3: SIWx register on x402scan
Sign a SIWx message with the 0x810f key (SIGNING ONLY — no funds move; locate key: `resolveIdentity`/`~/.openclaw/.env` EVM key or `~/.automaton/wallet.json`) → `POST https://www.x402scan.com/api/x402/registry/register {url}` with the signature. Verify: `GET https://api.cdp.coinbase.com/platform/v2/x402/discovery/resources` (paginate) OR x402scan listing shows the url/payTo. done = self-listing returns.

### Task 4: seed + reinforce
Self-buy 1 call (~$0.005 from a wallet NOT 0x810f to satisfy INV-7) → confirms CDP Bazaar auto-index. PR one line to an awesome-x402 list. Update spec + record first-discoverable evidence.

## Notes
- SIWx = wallet-ownership signature (auth), NOT a transaction — permitted, no funds move.
- Disk was at 10GB free 2026-07-26 — `npm install` for x402 deps before deploy; reclaim if needed.
- Prod `anicca-proxy` is a DIFFERENT app — do NOT mount here; deploy standalone.
